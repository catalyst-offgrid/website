---
import { Image } from "astro:assets";
import lockup from "../assets/images/logos/catalyst-white.png";
import { site } from "../data/site";
const currentPath = Astro.url.pathname.replace(/\/$/, "") || "/";
---
<header class="bg-brand-blue text-white">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 py-6 flex items-center justify-between gap-6">
    <a href="/" class="block shrink-0" aria-label="Catalyst Energy Advisors home">
      <Image src={lockup} alt="Catalyst Energy Advisors" class="h-11 w-auto" width={480} height={84} />
    </a>
    <nav aria-label="Primary">
      <ul class="flex items-center gap-2 sm:gap-7">
        {site.nav.map((item) => {
          const normalized = item.href.replace(/\/$/, "") || "/";
          const isActive = currentPath === normalized;
          return (
            <li>
              <a
                href={item.href}
                class:list={[
                  "text-base tracking-wide hover:text-brand-gold transition-colors",
                  isActive ? "text-brand-gold" : "text-white"
                ]}
                aria-current={isActive ? "page" : undefined}
              >
                {item.label}
              </a>
            </li>
          );
        })}
      </ul>
    </nav>
  </div>
</header>
