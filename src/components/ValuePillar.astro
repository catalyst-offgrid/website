---
interface Props { title: string; body: string; }
const { title, body } = Astro.props;
---
<div class="p-6 border-l-4 border-brand-gold bg-slate-50">
  <h3 class="font-display text-xl font-semibold text-brand-blue">{title}</h3>
  <p class="mt-3 text-slate-700 leading-relaxed">{body}</p>
</div>
